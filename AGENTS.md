# Codex 项目长期规则

本文件适用于仓库根目录及其所有子目录。Codex 在执行任何任务前必须先阅读并遵守本文件。

## 1. 分支规则

- 禁止直接在 `master` 或 `release/*` 分支开发。
- 每个任务必须创建独立任务分支，格式为 `task/TASK-编号-英文简称`。
- 新任务必须从最新 `origin/master` 创建，不得从旧任务分支继续开发新任务。
- 禁止 force push。
- 禁止删除或移动正式标签。
- 禁止自动合并 Pull Request。
- 未经明确授权，不得创建正式版本、GitHub Release 或发布资产。

## 2. 测试规则

每个任务完成前至少实际执行：

- 完整离线 `pytest`；
- Python `compileall` 编译检查；
- `git diff --check`；
- 与本任务有关的专项测试；
- 审核文件完整性检查；
- 敏感信息和禁止路径扫描。

除非任务明确授权：

- 不调用真实高德 API；
- 不修改真实业务文件；
- 不上传测试产生的 Excel、缓存、日志或数据库；
- 不宣称没有实际执行的测试已经通过。

任何测试、安全扫描或审核材料检查失败时必须停止，不得继续推送。

## 3. 数据安全规则

禁止提交或上传：

- 高德 Key、GitHub Token、密码、密钥、凭据、`.env`；
- DPAPI 或 Windows 凭据；
- 真实业务 Excel、业务结果 Excel；
- 客户或仓库完整地址、手机号、业务经纬度；
- 地址确认截图、可信地址库、普通驾车缓存；
- SQLite、`cache`、`logs`、`outputs`、`temp`、`release`；
- EXE、ZIP 或含敏感历史的 Bundle。

扫描命中时必须停止，不得推送。公开仓库中尤其禁止上传任何业务数据或凭据。

## 4. 既有业务保护规则

未经任务明确授权，不得修改：

- 报价类型必须精确等于“非线路报价”；
- 高德地理编码接口；
- 高德普通驾车接口及参数；
- 地址清洗规则；
- 多目的地规则和城市冲突规则；
- 可信地址库；
- 地址 ID 和状态；
- 路线缓存键和失效规则；
- 正式结果列名称；
- 原文件不覆盖；
- 非目标行不修改；
- Excel/WPS 安全保存；
- VBA 保留；
- 宏和事件禁用；
- “普通驾车不代表货车实际可通行”的提示。

## 5. 用户沟通规则

用户是入门使用者。需要用户选择时，最多提供两个选项并明确标记推荐方案，按以下顺序说明：

1. 发现了什么问题；
2. 会造成什么影响；
3. 推荐选择哪个方案；
4. 会删除或改变什么；
5. 是否影响程序正常使用；
6. 用户只需要回复哪一句话。

## 6. 总指挥固定审核通知

每个任务只有在以下条件全部满足后，才可以向用户宣称完成：

- 任务分支已经成功推送；
- Pull Request 已经创建或更新；
- 审核文件已经推送到 GitHub；
- 已读取 Pull Request 当前 HEAD 的完整 Commit Hash；
- 当前 HEAD 对应的 GitHub Actions 已经结束。

任务完成时，Codex 必须把 `scripts/finalize_task.ps1` 根据真实 Git、GitHub、
pytest 和安全扫描结果生成的 `【可直接复制给总指挥审核】` 完整通知放在最终回复
最末尾。用户只需要复制整段通知，不应再自行查找地址、编号或状态。

通知必须包含项目名称、任务编号和名称、仓库和 Pull Request 实际地址、PR 编号、
任务分支、PR 当前 HEAD、Actions 最终状态与运行信息、固定审核入口、任务审核目录、
pytest 真实数量、安全扫描真实数量、Draft/合并状态，以及 master、Release 和正式标签
是否发生变化。

强制规则：

- 任何字段不得保留尖括号占位符或要求用户补填；
- PR 地址、PR HEAD 或 Actions 状态无法取得时，不得输出完成通知；
- Actions 仍在运行时必须继续等待，不得提前输出通知；
- Actions 失败时必须明确写失败，不得写成成功或全部通过；
- 通知只能在审核文件已经推送后生成；
- 不自动合并 Pull Request，不自动修改 master，不自动创建 Release 或移动正式标签；
- `【可直接复制给总指挥审核】` 通知及其结束分隔线必须是 Codex 最终回复的最后内容。
