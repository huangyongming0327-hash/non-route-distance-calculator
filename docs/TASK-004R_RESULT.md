# TASK-004R 结果

完成日期：2026-07-20  
阶段状态：**已完成，暂停等待总指挥验收。**

## 总结

| 必答项 | 结果 |
| --- | --- |
| 普通驾车接口是否可用 | 是；Key、地理编码 v3、普通驾车 v5 连接测试通过 |
| 实际 HTTP 调用数量 | 连接 2；三线路 10；首次样表 56；总计 68；最终 Excel/WPS 缓存刷新均为 0 |
| 接口版本 | 路径规划 2.0 `/v5/direction/driving`，合同 `amap-driving-v5-contract-2026-06-17` |
| strategy | 32；同时固定 `cartype=0`、`ferry=1`、`alternative_route=1` |
| 三条测试路线距离 | 375.1、261.1、1766.3 公里 |
| 样表目标行 | 74 |
| 唯一起终点 | 28 |
| 缓存复用 | 首次样表 46；最终 Excel 73；最终 WPS 73 |
| 多目的地 | 1（行 248，不计算） |
| 城市冲突 | 3（行 39、78、272，按详细地址算路并标红） |
| Excel 输出 | 通过，`Excel_样表_普通驾车距离结果_待验收.xlsm` |
| WPS 输出 | 通过，`WPS_样表_普通驾车距离结果_待验收.xlsm` |
| 原文件保护 | SHA-256 不变；A:P、公式、图片、筛选、206 隐藏行、原条件格式均保留 |
| 自动测试 | 148 passed（含 Excel/WPS 注册检查），0 failed，0 skipped |
| 已知限制 | 普通驾车非货车路线；73 条成功行均需地址精度/冲突复核；多目的地不排序；候选路径实返 3 条但固定取第一条 |
| 是否建议进入最终产品化 | **有条件建议进入下一阶段评审，但不建议直接发布 V1.0** |

## 实现结果

- 程序和窗口名改为“非线路运距计算工具 V1.0 — 高德普通驾车距离版”。
- 正式链路不再调用 `/v4/direction/truck`，没有货车权限、size、物理参数、单位门或车辆歧义门。
- 正式结果只来自 v5 普通驾车；失败时不降级、不伪造。
- 车型可选且只读；空值、未知值和不同车型不影响请求和缓存。
- 独立 `driving_real.sqlite` 缓存；28 个唯一起终点动态统计，无写死 29/74。
- 新普通驾车三列表头完整连续；旧货车列识别测试证明不会被复用或覆盖。
- 正式说明完整写入每一条成功行，未出现“货车可通行”等错误宣称。
- UI 提供连接测试、强制刷新和清除正式路线缓存；mock 默认隐藏。

## 文件验收

两份文件均为真正的宏启用工作簿，宏未自动运行。每次保存均经过工作副本、检查点、专属 Office 进程归属验证、退出检查和只读重开；关键失败时不会提升为最终文件。

最终 Excel 文件大小 828654 字节，SHA-256 `898F890C8AEA057319F8352A5D0931250BB2A3725A189809EC76436E585E0327`。  
最终 WPS 文件大小 831745 字节，SHA-256 `ECF7DD03B55822D72BB02D7D117245B8B774C7673998DFD088B161DB88F26861`。

## 产品化建议

普通驾车 API、缓存、批处理和 Office 安全写回已达到进入下一阶段评审的技术条件。但样表中 70 条为低精度地理编码、3 条为城市冲突，说明正式产品还需要明确的人工复核流程、地址纠错入口和可接受精度政策。因此本结论是“有条件建议”，不是发布批准。

按任务要求，本阶段不继续 GUI 美化、EXE、PDF 或正式 V1.0 发布。

最终自动审计同时确认：源码不含 `/v4/direction/truck` 调用，最终输出不含错误货车宣称，安全存储目录之外未发现明文 Key。证据见 `docs/evidence/TASK004R_FINAL_AUDIT.json`。
