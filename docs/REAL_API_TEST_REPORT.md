# TASK-004 真实适配器测试报告

> **终止说明（2026-07-20）**：专业货车API因最低采购成本过高，于2026-07-20终止，项目改用普通驾车参考距离。本文仅作为历史技术记录，不代表当前实现。

文档日期：2026-07-20  
环境：Windows 11、Python 3.12  
真实高德 HTTP 调用：**0 次**

## 自动化结果

```text
$env:RUN_OFFICE_TESTS='1'
.\.venv\Scripts\python.exe -m pytest -q
120 passed in 66.83s
```

原阶段 3B 的 95 项（含 2 项 Excel/WPS COM 集成）全部保留通过；TASK-004 新增 25 项测试，合计 120 项。

新增覆盖：

- Key 安全存储优先级、内存降级、删除和脱敏显示；
- HTTP 日志删除 Key、URL 不含查询串、电话脱敏；
- 无效 Key、权限、日配额、QPS、网络/5xx 有界重试；
- 地理编码成功、无结果、结构异常、低精度和城市辅助规则；
- 详细地址含明确省市时不被城市列强制限制；
- size-only 与显式默认参数请求；
- paths/steps 原始距离、steps 汇总、容差与单一公里换算门；
- 车辆参数模式、单位版本和响应形态进入路线缓存键；
- 正式服务缓存先查后调用、real/mock 模式隔离；
- 正式输出说明不含模拟提示；
- 七项正式批量门阻断。

## 真实联调状态

本机 Credential Manager、项目 DPAPI 凭据文件和当前进程内存均未发现已配置 Key。依据任务“无 Key 时的处理”，没有调用地理编码或专业货车接口，没有生成 `logs/real_api_validation` 下的伪造响应，也没有生成真实 Excel/WPS 结果。

真实响应结构、当前 Key 权限、实际配额、三条路线距离、车辆参数对照和高德工单结论仍未验证。
