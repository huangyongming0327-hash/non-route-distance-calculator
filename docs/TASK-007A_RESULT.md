# TASK-007A 结果：V1.0 RC2 待实机验收

## 结论

TASK-007A 已完成代码改造、专项测试、全量离线回归和 V1.0 RC2 便携包构建。当前样表首次检测 80.39 ms、缓存命中 22.07 ms；自动检测和输出目录权限检查均不再阻塞 Qt 主线程。业务规则、API、业务缓存、结果口径和 Excel/WPS 保存逻辑未修改。

本次没有直接发布新的正式 V1.0。RC2 现暂停，等待用户再次实机验收。

## 需求完成情况

- [x] 输入文件点击、对话框返回、工作表列表、表头、字段、预览分阶段日志。
- [x] 输出目录点击、对话框返回、权限检查分阶段日志。
- [x] Office 检测与 SQLite 加载耗时日志。
- [x] 输出目录选择后只更新字符串和界面，权限探针在后台运行并设 3 秒等待上限。
- [x] 工作簿检测全部进入 Qt 后台线程，不使用 Excel/WPS COM。
- [x] 只分析选中工作表；表头默认仅前 20 行；样例最多 30 行；预览前 10 行。
- [x] 四阶段中文进度、取消、忙碌状态、完成恢复、中文失败原因。
- [x] 防重复检测、检测中换文件、旧结果隔离和重复错误抑制。
- [x] 文件签名 + 工作表 + 算法版本检测缓存，文件变化自动失效。
- [x] 缓存只保存结构结果和少量脱敏样例，不保存完整业务内容。
- [x] 281/2,000/10,000 行、表头 1/4/15、中文/空格路径及全部指定交互场景测试。
- [x] 全程未调用真实高德 API；表头检测未启动 Excel/WPS。

## 自动测试与构建验证

| 项目 | 结果 |
|---|---|
| 全量离线 pytest | 200 passed，4 skipped，0 failed |
| TASK-007A 专项测试 | 11 passed |
| 当前 281 行首次 / 缓存 | 80.39 ms / 22.07 ms |
| 2,000 行首次 / 缓存 | 192.40 ms / 79.34 ms |
| 10,000 行首次 / 缓存 | 822.59 ms / 387.15 ms |
| 中文/空格路径便携启动 | 3/3 通过 |
| 便携主窗口最慢出现 | 2.752 秒 |
| 发布目录 / ZIP 安全扫描 | 191 / 191 文件，0 命中 |

详细证据：

- `docs/PERFORMANCE_OPTIMIZATION_REPORT.md`
- `docs/evidence/task007a_rc1_baseline.json`
- `docs/evidence/task007a_performance.json`
- `docs/evidence/task007a/portable_verification_rc2.json`
- `docs/evidence/task007a/security_scan_rc2.json`

## RC2 交付物

- 便携目录：`release/非线路运距计算工具_V1.0_RC2/`
- EXE：`release/非线路运距计算工具_V1.0_RC2/非线路运距计算工具.exe`
- ZIP：`release/非线路运距计算工具_V1.0_RC2_便携版.zip`
- EXE SHA-256：`40773AAC2827F74675EFFD6EB983DF1AAF08726152B49BC9A615D225944FD37C`
- ZIP SHA-256：`708C8861685449F9832D808E23E618B7364FA65C4FA78D5C52D513235798CF22`

构建后的 `VERSION.txt` 明确标记 `V1.0 RC2`、基线提交和“含未提交的 TASK-007A 候选版修改”，避免把候选包误认为正式发布。

## 用户复测重点

1. 选择包含大量文件的输出目录，确认对话框关闭后窗口立即恢复，状态文字随后更新权限结果。
2. 首次点击“自动检测表头/字段”时移动窗口、切换页签，并尝试取消。
3. 快速连续点击检测，以及检测过程中更换文件，确认没有重复任务或旧结果覆盖。
4. 对同一未变化文件再次检测，确认显示“检测缓存”且尽量在 1 秒内完成。
5. 修改并保存文件后再次检测，确认缓存失效并重新分析。
6. 如仍有等待，保留 `logs/performance_task007a.jsonl` 供阶段定位。

RC2 到此暂停，等待用户实机验收结论。
